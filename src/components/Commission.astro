<section class="commission">
  <div id="commission" class="commission__band">
    <div class="panel-inner">
      <div class="commission__wrap">
        <p class="commission__kicker" data-reveal>Commissions</p>

        <h2 class="commission__title" data-reveal>
          <span class="commission__title-line">A piece made</span>
          <span class="commission__title-accent">for you</span><span class="commission__title-line">.</span>
        </h2>

        <div class="commission__body" data-reveal>
          <p>The most special artworks hold a personal connection.</p>
          <p>Let’s build yours.</p>
        </div>

        <a
          class="commission__button"
          href="#get-in-touch"
          data-contact-trigger
          data-contact-topic="commissions"
          data-contact-subject="Commission inquiry"
          data-contact-message="Share your vision — subject, mood, colours, space it’s for..."
          data-reveal
        >
          Inquire about a commission &rarr;
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  .commission {
    --band-pink: #ffa5ab;
    --band-navy: #07294a;
  }

  /* Same band pattern as Sip & Paint — full-width colour block */
  .commission__band {
    position: relative;
    margin: clamp(2rem, 5vw, 4rem) 0;
    padding: clamp(5.5rem, 12vw, 9rem) 0;
    background: var(--band-pink);
    scroll-margin-top: calc(var(--scroll-anchor-offset) + var(--space-4));
  }

  .commission__wrap {
    max-width: var(--content-rail);
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
  }

  .commission__kicker {
    margin: 0 0 var(--space-3);
    font-family: var(--font-mono);
    font-size: 12px;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: #ffffff;
  }

  .commission__title {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    column-gap: 0.28em;
    margin: 0 0 var(--space-4);
    font-family: var(--font-display);
    font-weight: 400;
    font-size: var(--text-display-sm);
    line-height: 0.95;
    letter-spacing: 0.01em;
    text-transform: uppercase;
  }

  .commission__title-line {
    color: #ffffff;
  }

  .commission__title-accent {
    color: var(--band-navy);
  }

  .commission__body {
    margin: 0 0 var(--space-6);
    max-width: 38ch;
  }

  .commission__body p {
    margin: 0;
    font-size: var(--text-lead);
    line-height: 1.65;
    color: #ffffff;
  }

  .commission__button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 56px;
    padding: 16px clamp(32px, 6vw, 56px);
    border-radius: 0;
    background: var(--band-navy);
    color: #ffffff;
    font-family: var(--font-mono);
    font-size: 12px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    text-decoration: none;
    transition:
      background var(--duration-fast) var(--ease-out),
      transform var(--duration-fast) var(--ease-out);
  }

  .commission__button:hover {
    background: #0a355c;
  }

  .commission__button:active {
    transform: scale(0.98);
  }

  @media (max-width: 767px) {
    /* Match veil gutter — no 100vw (overflow fights iOS scroll). */
    .commission__band {
      margin-left: calc(-1 * var(--space-2));
      margin-right: calc(-1 * var(--space-2));
      margin-top: var(--space-4);
      margin-bottom: var(--space-4);
      padding: var(--space-8) var(--space-3);
    }

    .commission__band .panel-inner {
      padding-left: var(--space-3);
      padding-right: var(--space-3);
    }

    .commission__title {
      flex-direction: column;
      align-items: center;
    }

    .commission__button {
      width: 100%;
      max-width: 340px;
    }
  }
</style>
