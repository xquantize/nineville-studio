<nav class="nav" aria-label="Main navigation">
  <div class="nav__inner">
    <a href="#" class="nav__brand">Nineville Studio</a>

    <button
      type="button"
      class="nav__toggle"
      aria-label="Open menu"
      aria-expanded="false"
      aria-controls="nav-menu"
    >
      <svg class="nav__icon nav__icon--open" width="24" height="24" viewBox="0 0 24 24" aria-hidden="true">
        <line x1="4" y1="7" x2="20" y2="7" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" />
        <line x1="4" y1="12" x2="20" y2="12" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" />
        <line x1="4" y1="17" x2="20" y2="17" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" />
      </svg>
      <svg class="nav__icon nav__icon--close" width="24" height="24" viewBox="0 0 24 24" aria-hidden="true">
        <line x1="5" y1="5" x2="19" y2="19" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" />
        <line x1="19" y1="5" x2="5" y2="19" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" />
      </svg>
    </button>

    <div id="nav-menu" class="nav__links">
      <a href="#bio" class="nav__link" data-section="bio">Bio</a>
      <a href="#mediums" class="nav__link" data-section="mediums">Mediums</a>
      <a href="#art" class="nav__link" data-section="art">Art</a>
      <a href="#commission" class="nav__link" data-section="commission">Commission</a>
    </div>
  </div>
</nav>

<script>
  const nav = document.querySelector('.nav');
  const toggle = document.querySelector('.nav__toggle');
  const links = document.querySelectorAll('.nav__link');

  function closeMenu() {
    nav?.classList.remove('nav--open');
    toggle?.setAttribute('aria-expanded', 'false');
    toggle?.setAttribute('aria-label', 'Open menu');
    document.body.style.overflow = '';
  }

  toggle?.addEventListener('click', () => {
    const isOpen = nav?.classList.toggle('nav--open');
    toggle.setAttribute('aria-expanded', String(isOpen));
    toggle.setAttribute('aria-label', isOpen ? 'Close menu' : 'Open menu');
    document.body.style.overflow = isOpen ? 'hidden' : '';
  });

  links.forEach((link) => {
    link.addEventListener('click', closeMenu);
  });

  const onScroll = () => {
    nav?.classList.toggle('nav--scrolled', window.scrollY > 24);
  };
  onScroll();
  window.addEventListener('scroll', onScroll, { passive: true });

  const sections = ['bio', 'mediums', 'art', 'commission']
    .map((id) => document.getElementById(id))
    .filter(Boolean) as HTMLElement[];

  if (sections.length && !window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            const id = entry.target.id;
            links.forEach((link) => {
              link.classList.toggle('is-active', link.getAttribute('data-section') === id);
            });
          }
        });
      },
      { rootMargin: '-40% 0px -50% 0px', threshold: 0 }
    );

    sections.forEach((section) => observer.observe(section));
  }
</script>

<style>
  .nav {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 100;
    background: var(--panel-bg);
    backdrop-filter: blur(12px) saturate(1.2);
    -webkit-backdrop-filter: blur(12px) saturate(1.2);
    border-bottom: 0.5px solid rgba(184, 154, 106, 0.2);
    transition:
      background var(--duration-normal) var(--ease-out),
      box-shadow var(--duration-normal) var(--ease-out),
      border-color var(--duration-normal) var(--ease-out);
  }

  .nav--scrolled {
    background: var(--panel-bg-strong);
    box-shadow: 0 4px 24px rgba(43, 36, 25, 0.06);
    border-bottom-color: rgba(184, 154, 106, 0.35);
  }

  .nav__inner {
    max-width: var(--content-max);
    margin: 0 auto;
    padding: 0 var(--content-padding);
    display: flex;
    align-items: center;
    justify-content: space-between;
    min-height: 56px;
  }

  .nav__brand {
    font-family: var(--font-serif);
    font-size: 16px;
    text-decoration: none;
    color: var(--sand-900);
    transition: opacity var(--duration-fast) var(--ease-out);
  }

  .nav__brand:hover {
    opacity: 0.7;
  }

  .nav__toggle {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 44px;
    height: 44px;
    padding: 0;
    border: none;
    background: transparent;
    color: var(--sand-900);
    cursor: pointer;
    transition: opacity var(--duration-fast) var(--ease-out);
  }

  .nav__toggle:hover {
    opacity: 0.65;
  }

  .nav__icon {
    transition: transform var(--duration-normal) var(--ease-out);
  }

  .nav__icon--close {
    display: none;
  }

  .nav--open .nav__icon--open {
    display: none;
  }

  .nav--open .nav__icon--close {
    display: block;
  }

  .nav__links {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: var(--space-6);
    position: fixed;
    inset: 0;
    top: 56px;
    background: var(--panel-bg-strong);
    backdrop-filter: blur(20px) saturate(1.2);
    -webkit-backdrop-filter: blur(20px) saturate(1.2);
    z-index: 99;
    opacity: 0;
    visibility: hidden;
    pointer-events: none;
    transform: translateY(-8px);
    transition:
      opacity var(--duration-normal) var(--ease-out),
      visibility var(--duration-normal) var(--ease-out),
      transform var(--duration-normal) var(--ease-out);
  }

  .nav--open .nav__links {
    opacity: 1;
    visibility: visible;
    pointer-events: auto;
    transform: translateY(0);
  }

  .nav__link {
    font-family: var(--font-serif);
    font-size: 28px;
    letter-spacing: -0.01em;
    text-decoration: none;
    color: var(--sand-900);
    transition:
      color var(--duration-fast) var(--ease-out),
      opacity var(--duration-fast) var(--ease-out),
      transform var(--duration-normal) var(--ease-out);
    opacity: 0;
    transform: translateY(12px);
  }

  .nav--open .nav__link {
    opacity: 1;
    transform: translateY(0);
  }

  .nav--open .nav__link:nth-child(1) { transition-delay: 60ms; }
  .nav--open .nav__link:nth-child(2) { transition-delay: 110ms; }
  .nav--open .nav__link:nth-child(3) { transition-delay: 160ms; }
  .nav--open .nav__link:nth-child(4) { transition-delay: 210ms; }

  .nav__link:hover,
  .nav__link.is-active {
    color: var(--sand-500);
  }

  @media (min-width: 768px) {
    .nav__toggle {
      display: none;
    }

    .nav__links {
      position: static;
      flex-direction: row;
      align-items: center;
      gap: var(--space-4);
      background: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      opacity: 1;
      visibility: visible;
      pointer-events: auto;
      transform: none;
      inset: auto;
    }

    .nav__link {
      font-family: var(--font-sans);
      font-size: 13px;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      opacity: 1;
      transform: none;
      transition: color var(--duration-fast) var(--ease-out);
      transition-delay: 0ms;
    }

    .nav__link::after {
      content: '';
      display: block;
      width: 0;
      height: 1px;
      margin-top: 2px;
      background: var(--sand-500);
      transition: width var(--duration-normal) var(--ease-out);
    }

    .nav__link:hover::after,
    .nav__link.is-active::after {
      width: 100%;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .nav__links,
    .nav__link {
      transition: none;
    }

    .nav--open .nav__link {
      transition-delay: 0ms;
    }
  }
</style>
