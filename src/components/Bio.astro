<section id="bio" class="bio">
  <div class="bio__band">
    <div class="panel-inner">
      <div class="bio__wrap">
        <header class="bio__header">
          <p class="section-kicker" data-reveal>Meet the artist</p>
          <h2 class="section-heading bio__title" data-reveal>
            <span class="bio__title-about">About</span>
            <span class="bio__title-name">Laura</span>
          </h2>
        </header>

        <figure class="bio__portrait" data-reveal>
          <span class="bio__portrait-plate" aria-hidden="true"></span>
          <span class="bio__portrait-mat">
            <span class="bio__portrait-clip">
              <img
                class="bio__portrait-img"
                src="/images/bio-profile.webp"
                alt="Laura Neundörfer"
                width={1000}
                height={993}
                loading="eager"
                decoding="async"
                fetchpriority="low"
              />
            </span>
          </span>
        </figure>

        <div class="bio__text" data-reveal>
          <p class="bio__lead">
            Laura is a mixed-media artist creating textured artworks that bridge the gap between
            structure and fluid mediums.
          </p>
          <p>
            For the first part of her life, her world was defined by systems and structures in
            Germany. It was a predictable rhythm that kept her safe, but left little space for
            creativity.
          </p>
          <p>
            What began as a passion for surfing evolved into a whole new way of living: She left the
            landlocked lifestyle behind, moved abroad to the coast and allowed herself to be guided
            by creativity, freedom and flow.
          </p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .bio {
    --bio-blue: #07294a;
    --bio-gap: clamp(3.5rem, 7vw, 6.5rem);
    --bio-portrait: clamp(11.5rem, 20vw, 17rem);
    --bio-tilt: -1.6deg;
    scroll-margin-top: var(--scroll-anchor-offset);
  }

  /* Soft edge fades, solid navy holds most of the band. */
  .bio__band {
    position: relative;
    margin: 0 0 clamp(3.5rem, 7vw, 6rem);
    padding: clamp(4.75rem, 7.5vw, 7rem) 0 clamp(5.5rem, 10vw, 9rem);
    background: linear-gradient(
      180deg,
      rgba(7, 41, 74, 0) 0%,
      rgba(7, 41, 74, 0.65) 10%,
      var(--bio-blue) 18%,
      var(--bio-blue) 82%,
      rgba(7, 41, 74, 0.65) 90%,
      rgba(7, 41, 74, 0) 100%
    );
  }

  .bio__wrap {
    max-width: var(--content-rail);
    margin: 0 auto;
    display: grid;
    grid-template-columns: var(--bio-portrait) minmax(0, 1fr);
    grid-template-areas:
      'portrait header'
      'portrait text';
    column-gap: var(--bio-gap);
    row-gap: var(--space-3);
    align-items: start;
  }

  .bio__header {
    grid-area: header;
    text-align: left;
    min-width: 0;
    padding-left: clamp(0.5rem, 2vw, 1.75rem);
  }

  .bio__band .section-kicker {
    color: rgba(255, 255, 255, 0.55);
  }

  .bio__title {
    display: flex;
    flex-direction: row;
    align-items: baseline;
    justify-content: flex-start;
    gap: clamp(0.45rem, 1.4vw, 1rem);
    line-height: 0.9;
    font-size: var(--text-display);
    text-transform: uppercase;
    margin: 0;
  }

  .bio__title-about {
    color: #ffffff;
  }

  .bio__title-name {
    color: #ff7f89;
  }

  .bio__lead {
    color: #ffffff;
    font-weight: 700;
  }

  .bio__portrait {
    --bio-radius: 10px;
    grid-area: portrait;
    position: relative;
    width: 100%;
    max-width: var(--bio-portrait);
    aspect-ratio: 1000 / 993;
    margin: 0;
    padding: 0;
    border: none;
    background: none;
    box-sizing: border-box;
    justify-self: stretch;
    align-self: center;
    isolation: isolate;
    z-index: 1;
    transform: rotate(var(--bio-tilt));
  }

  .bio__portrait-plate {
    position: absolute;
    inset: -0.45rem -0.5rem -0.55rem -0.4rem;
    z-index: -1;
    border-radius: calc(var(--bio-radius) + 2px);
    background: linear-gradient(145deg, #0b3a66 0%, #07294a 100%);
    box-shadow: 0 16px 40px rgba(0, 0, 0, 0.38);
    transform: rotate(2.4deg) translate(0.12rem, 0.18rem);
  }

  .bio__portrait-mat {
    position: relative;
    display: block;
    width: 100%;
    height: 100%;
    box-sizing: border-box;
    padding: 2px;
    border-radius: var(--bio-radius);
    overflow: hidden;
    background: linear-gradient(
      135deg,
      rgba(255, 255, 255, 0.95) 0%,
      rgba(255, 127, 137, 0.92) 42%,
      rgba(255, 255, 255, 0.78) 72%,
      rgba(255, 127, 137, 0.85) 100%
    );
    box-shadow: 0 10px 28px rgba(0, 0, 0, 0.28);
  }

  .bio__portrait-clip {
    position: relative;
    display: block;
    width: 100%;
    height: 100%;
    border-radius: calc(var(--bio-radius) - 2px);
    overflow: hidden;
    background: #0a1018;
  }

  .bio__portrait-img {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center center;
  }

  .bio__text {
    grid-area: text;
    min-width: 0;
    text-align: left;
    padding-top: 0.15rem;
    /* Extra breath from the portrait beyond the column gap */
    padding-left: clamp(0.5rem, 2vw, 1.75rem);
  }

  .bio__text p {
    margin: 0 0 var(--space-3);
    font-size: clamp(16px, 0.35vw + 15px, 18px);
    line-height: 1.7;
    color: rgba(251, 246, 234, 0.9);
    max-width: none;
  }

  .bio__text p:last-child {
    margin-bottom: 0;
  }

  /* Comfortable reading measure on wide desktops — avoid ultra-long lines */
  @media (min-width: 1100px) {
    .bio__text {
      text-align: left;
      text-wrap: pretty;
    }
  }

  /* Mid tablets: keep two columns, slightly tighter portrait */
  @media (min-width: 768px) and (max-width: 999px) {
    .bio {
      --bio-portrait: clamp(12rem, 28vw, 15rem);
      --bio-gap: clamp(2.75rem, 5vw, 4rem);
    }

    .bio__text {
      padding-left: clamp(0.35rem, 1.5vw, 1rem);
    }

    .bio__text p {
      max-width: none;
    }
  }

  @media (max-width: 767px) {
    .bio {
      --bio-portrait: clamp(10.5rem, 42vw, 13rem);
      --bio-gap: var(--space-4);
    }

    .bio__band {
      margin-left: calc(-1 * var(--space-2));
      margin-right: calc(-1 * var(--space-2));
      margin-top: 0;
      margin-bottom: clamp(2.5rem, 6vw, 4rem);
      padding-top: clamp(2.5rem, 8vw, 4rem);
      background: linear-gradient(
        180deg,
        rgba(7, 41, 74, 0) 0%,
        rgba(7, 41, 74, 0.35) 14%,
        var(--bio-blue) 32%,
        var(--bio-blue) 78%,
        rgba(7, 41, 74, 0.55) 90%,
        rgba(7, 41, 74, 0) 100%
      );
    }

    .bio__band .panel-inner {
      padding-left: var(--space-3);
      padding-right: var(--space-3);
    }

    .bio__wrap {
      grid-template-columns: 1fr;
      grid-template-areas:
        'header'
        'portrait'
        'text';
      justify-items: center;
      row-gap: var(--bio-gap);
    }

    .bio__header {
      justify-self: stretch;
      text-align: center;
      padding-left: 0;
    }

    .bio__title {
      justify-content: center;
      flex-wrap: wrap;
      row-gap: 0.1rem;
    }

    .bio__portrait {
      justify-self: center;
    }

    .bio__text {
      justify-self: stretch;
      text-align: left;
      padding-top: 0;
      padding-left: 0;
    }

    .bio__text p {
      max-width: none;
      font-size: 16px;
      line-height: 1.65;
      margin-bottom: var(--space-2);
    }
  }

  /* Short landscape phones: side-by-side without huge portrait */
  @media (max-width: 900px) and (orientation: landscape) and (max-height: 500px) {
    .bio {
      --bio-portrait: clamp(7.5rem, 28vh, 10rem);
      --bio-gap: var(--space-3);
    }

    .bio__wrap {
      grid-template-columns: var(--bio-portrait) minmax(0, 1fr);
      grid-template-areas:
        'portrait header'
        'portrait text';
      justify-items: stretch;
      row-gap: var(--space-1);
    }

    .bio__header {
      text-align: left;
    }

    .bio__title {
      justify-content: flex-start;
      font-size: clamp(2rem, 8vh, 3rem);
    }

    .bio__band {
      padding-top: var(--space-4);
      padding-bottom: var(--space-4);
      margin-bottom: var(--space-4);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .bio__portrait {
      transform: none;
    }

    .bio__portrait-plate {
      transform: none;
    }
  }
</style>
