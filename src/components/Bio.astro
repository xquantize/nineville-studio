<section class="panel">
  <div class="panel-inner">
    <h2 id="bio" class="section-heading" data-reveal>Bio</h2>

    <div class="bio__content">
      <figure class="bio__portrait" data-reveal>
        <img
          src="/images/bio-portrait.webp"
          alt="Laura Neundörfer"
          width={440}
          height={521}
          loading="lazy"
          decoding="async"
        />
      </figure>

      <div class="bio__text" data-reveal>
        <p>[PLACEHOLDER: Bio paragraph 1 — replace with Laura's words]</p>
      </div>
    </div>
  </div>
</section>

<style>
  .bio__content {
    max-width: 40rem;
  }

  .bio__content::after {
    content: '';
    display: table;
    clear: both;
  }

  .bio__portrait {
    float: left;
    width: 6.75rem;
    margin: 0.2rem var(--space-3) var(--space-2) 0;
    padding: 0.3rem;
    background: var(--card-bg);
    border: 0.5px solid rgba(184, 154, 106, 0.35);
    border-radius: 4px;
    box-sizing: border-box;
  }

  .bio__portrait img {
    display: block;
    width: 100%;
    height: auto;
    border-radius: 2px;
    border: 0.5px solid rgba(184, 154, 106, 0.22);
  }

  .bio__text p {
    margin: 0 0 var(--space-3);
    font-size: 16px;
    line-height: 1.65;
  }

  .bio__text p:last-child {
    margin-bottom: 0;
  }

  @media (min-width: 768px) {
    .bio__portrait {
      width: 9.25rem;
      margin-right: var(--space-4);
      padding: 0.35rem;
    }
  }

  @media (max-width: 767px) {
    .bio__portrait {
      width: 5.75rem;
      margin-right: var(--space-2);
      padding: 0.25rem;
    }

    .bio__text p {
      font-size: 15px;
      line-height: 1.6;
    }
  }
</style>
